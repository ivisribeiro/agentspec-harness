{
  "id": "G_ROUTER_COVERAGE-block",
  "gate": "G_ROUTER_COVERAGE",
  "expect": "block",
  "args": { "agents": "agents", "routing": "routing.json" },
  "note": "an agent on disk is missing from the routing table — a silent-skip data-loss hole"
}
